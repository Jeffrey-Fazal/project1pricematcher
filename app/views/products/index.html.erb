<h1>All Products</h1>

<p>View our products and subscribe for the latest deals.</p>
   <div class="album py-5 bg-light">
    <div class="container">

      <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
        <% @products.each do |product| %>
        <div class="col">
          <div class="card shadow-sm">
            <%= if product.image_url.nil?
              img_url = 'https://placehold.co/400/orange/white?text=' + product.name 
              image_tag img_url , class:"card-img-top"
            else
              image_tag product.image_url , class:"card-img-top"
            end
            %>
            <div class="card-body">
              <p class="card-text"><%= product.name unless product.name === !nil %></p>
              <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                  <%= link_to 'view',  product, class: "btn btn-sm btn-outline-secondary" %>
                  <%= link_to 'best match',  product, class: "btn btn-sm btn-outline-secondary" %>
                </div>
                <small class="text-muted">Cheapest Price: <%= number_to_currency(product.price_cheaptest) unless product.price_cheaptest === !nil %></small>
              </div>
            </div>
          </div>
        </div>
        <!-- End of thumb | Copy from col div to here to replicate -->
        <% end %>
      </div>
    </div>
  </div>

<% @products.each do |product| %>
    <p>Name: <%= link_to product.name,  product %></p>
<% end %>